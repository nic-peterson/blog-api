[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run prisma:migrate:deploy --workspace @blog/api && npm run start --workspace @blog/api"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
